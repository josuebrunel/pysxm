[tox]
envlist = py27, py35

[testenv]
deps=
    pytest
    coverage
    lxml
    python-dateutil
commands=
    - python -m coverage run --source=pysxm -m pytest -vs tests/
    - python -m coverage report -m
